{
  "name": "gages-through-the-ages",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "serve": "env NODE_OPTIONS=--openssl-legacy-provider vue-cli-service serve --mode test_tier",
    "serve-test": "Nenv NODE_OPTIONS=--openssl-legacy-provider ODE_ENV=development vue-cli-service serve --mode test_tier",
    "serve-qa": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=development vue-cli-service serve --mode qa_tier",
    "serve-beta": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=development vue-cli-service serve --mode beta_tier",
    "serve-prod": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=development vue-cli-service serve --mode prod_tier",
    "build": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=production vue-cli-service build --mode prod_tier",
    "build-test": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=production vue-cli-service build --mode test_tier",
    "build-qa": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=production vue-cli-service build --mode qa_tier",
    "build-beta": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=production vue-cli-service build --mode beta_tier",
    "build-prod": "env NODE_OPTIONS=--openssl-legacy-provider NODE_ENV=production vue-cli-service build --mode prod_tier",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^1.2.36",
    "@fortawesome/free-brands-svg-icons": "^5.15.4",
    "@fortawesome/free-solid-svg-icons": "^5.15.4",
    "@fortawesome/vue-fontawesome": "^2.0.0",
    "beerslider": "^1.0.3",
    "core-js": "^3.6.5",
    "dns": "^0.2.2",
    "jquery": "^3.5.1",
    "mapbox-gl": "^1.12.0",
    "mapbox-gl-compare": "^0.4.0",
    "uswds": "^2.8.1",
    "vue": "^2.6.12",
    "vue-analytics": "^5.22.1",
    "vue-browser-detect-plugin": "^0.1.13",
    "vue-mapbox": "^0.4.1",
    "vue-mobile-detection": "^1.0.0",
    "vue-range-slider": "^0.6.0",
    "vue-router": "^3.4.3",
    "vuex": "^3.5.1"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "^4.5.4",
    "@vue/cli-plugin-eslint": "^4.5.4",
    "@vue/cli-service": "^4.5.4",
    "babel-eslint": "^10.1.0",
    "eslint": "^7.7.0",
    "eslint-plugin-vue": "^6.2.2",
    "sass": "^1.62.1",
    "sass-loader": "^9.0.3",
    "vue-svg-loader": "^0.16.0",
    "vue-template-compiler": "^2.6.14"
  }
}
